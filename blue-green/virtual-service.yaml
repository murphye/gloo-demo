apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: default
  namespace: gloo-system
spec:
  virtualHost:
    domains:
    - todo.mattel-west.gsolo.net
    routes:
    - matchers:
      - prefix: /
      routeAction:
        single:
          upstream:
            name: todo-service-group
            namespace: todo


# TODO Add routes to the web interface for both v1 and v2 and blue/green???